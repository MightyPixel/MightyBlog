
{% extends "index.html" %}

{% block title %}Articles{% endblock %}

{% block content %}

    <h1>{{ post.title }}</h1>
    <h2>{{ post.date }}</h2>
    
    <p>
        {{ post.text }}
    </p>

    {% if comments %}
        <h1>Comments:</h1>
        <hr>
        {% for comment in comments %}
            <h2>Author:{{ comment.author }}</h2>
            {{ comment.text }}
        {% endfor %}
    {% endif %}

{% endblock %}
