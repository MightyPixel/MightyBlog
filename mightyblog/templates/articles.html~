{% extends "index.html" %}

{% block title  %}
    Articles
{% endblock %}

{% block aside_title %}Categories:{% endblock %}
{% block aside_content %} 
    {% for name, url in categories %}
        <a href="{{ url }}"><h2>{{ name }}</h2></a>
    {% endfor %}
{% endblock %}

{% block content %}
    <span class="my_h1">Articles:</span>
    <hr>
    <ul>
        {% for title, url, description, categories in posts %}
        <li><h2>
                <a href='{{ url }}' class="pix_link">
                    {{ title }}
                </a>
            </h2>
            <h3>
                {% for category in categories %}
                {{ category.name}}
                {% endfor %}
            </h3>
            -{{ description }}
        </li>
        {% endfor %}
    </ul>
{% endblock %}

